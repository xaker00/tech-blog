<div>
  {{#each posts}}

    <div class="row">
      <div class="col s12">
        <div class="card blue-grey darken-1">
          <div class="card-content white-text">
            <span class="card-title">{{this.title}}</span>
            <p>{{body}}</p>
          </div>
          <div class="card-action">
            {{#each comments}}
              <p>{{this.comment}} -{{this.userId}}</p>
            {{/each}}
          </div>
        </div>
      </div>
    </div>

  {{/each}}
</div>